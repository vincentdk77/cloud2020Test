server:
  port: 9001

spring:
  application:
    name: nacos-payment-provider
  cloud:
    nacos:
      discovery:
#        server-addr: localhost:8848 #配置Nacos单机地址
        server-addr: dk100:1111 #配置Nacos集群地址，nginx映射

management:
  endpoints:
    web:
      exposure:
        include: '*'